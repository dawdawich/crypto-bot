package space.dawdawich.model.constants

/**
 * Enum class representing strategies for detect analyzer's search algorithm.
 */
enum class AnalyzerChooseStrategy {
    BIGGEST_BY_MONEY, MOST_STABLE, CUSTOM, TEST
}
